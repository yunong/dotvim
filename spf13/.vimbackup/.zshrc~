# Path to your oh-my-zsh configuration.
ZSH=$HOME/.oh-my-zsh

# Set name of the theme to load.
# Look in ~/.oh-my-zsh/themes/
# Optionally, if you set this to "random", it'll load a random theme each
# time that oh-my-zsh is loaded.
ZSH_THEME="rkj-repos"

# Example aliases
# alias zshconfig="mate ~/.zshrc"
# alias ohmyzsh="mate ~/.oh-my-zsh"

# Set to this to use case-sensitive completion
# CASE_SENSITIVE="true"

# Comment this out to disable weekly auto-update checks
# DISABLE_AUTO_UPDATE="true"

# Uncomment following line if you want to disable colors in ls
# DISABLE_LS_COLORS="true"

# Uncomment following line if you want to disable autosetting terminal title.
# DISABLE_AUTO_TITLE="true"

# Uncomment following line if you want red dots to be displayed while waiting for completion
# COMPLETION_WAITING_DOTS="true"

# Which plugins would you like to load? (plugins can be found in ~/.oh-my-zsh/plugins/*)
# Custom plugins may be added to ~/.oh-my-zsh/custom/plugins/
# Example format: plugins=(rails git textmate ruby lighthouse)
plugins=(git npm brew osx gnu-utils vi-mode history-substring-search node extract github cloudapp gem git-flow autojump)


source $ZSH/oh-my-zsh.sh

# Customize to your needs...
export PATH=.../operator-toolkit/bin:/usr/local/bin:/Users/yjxiao/bin/:/usr/local/pgsql/bin/:/usr/bin:/Users/yunong/Scripts/:/usr/local/mysql/bin/:/Users/yunong/workspace/operator-toolkit/bin:/Users/yunong/workspace/postgres-replication/bin/:/usr/local/share/npm/bin:/usr/bin:/bin:/usr/sbin:/sbin:/usr/local/bin:/usr/X11/bin:/usr/local/git/bin:/opt/local/bin

# Custom aliases
alias lock='/System/Library/CoreServices/Menu\ Extras/User.menu/Contents/Resources/CGSession -suspend'
alias vol='set volume $1 | xargs osascript -e'
export CLICOLOR=1
export LSCOLORS=ExFxCxDxBxegedabagacad
export NODE_PATH="/usr/local/lib/jsctags/:/usr/local/lib:/usr/local/lib/node:/usr/local/lib/node_modules/"
export P4EDITOR=vim
export P4PORT=perforce:9591
export P4CONFIG=.p4config
export SHELL=/bin/zsh


alias cloud9='/Developer/Applications/cloud9/bin/cloud9.sh'
alias lt='ls -lhrt'
alias ll='ls -l'
alias la='ls -la'

#
# Joyent CLI args
#
export SDC_CLI_URL=https://us-west-1.api.joyentcloud.com
export SDC_CLI_ACCOUNT=yunong
export SDC_CLI_KEY_ID=mac-pro-rsa
export SDC_CLI_IDENTITY=/Users/yunong/.ssh/id_rsa.pub

export MAPI_CREDENTIALS='admin:tot@ls3crit'

export AMON_URL=http://localhost:8080
export PATH=.../operator-toolkit/bin:$PATH

export HN='root@10.99.99.7'

alias joyent='ssh admin@8.19.32.182'
alias ws='cd ~/workspace'
alias pql='cd /usr/local/pgsql'
alias debug='node-inspector --web-port=10001'
alias headnode='ssh root@10.99.99.7'
alias ubu='ssh yunong@10.88.88.131'

alias nash='env NODE_NO_READLINE=1 rlwrap -p Red -S "${HOSTNAME}: $(basename ${PWD})$(parse_git_branch) node $> " node'

alias lsearch='LDAPTLS_REQCERT=allow ldapsearch -x -LLL -D cn=root -w secret -H ldap://localhost:23455'
alias ladd='LDAPTLS_REQCERT=allow ldapadd -x -D cn=root -w secret -H ldap://localhost:23455'
alias lmodify='LDAPTLS_REQCERT=allow ldapmodify -x -D cn=root -w secret -H ldap://localhost:23455'
alias ldelete='LDAPTLS_REQCERT=allow ldapdelete -x -D cn=root -w secret -H ldap://localhost:23455'
